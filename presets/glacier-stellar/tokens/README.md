# Portable preset tokens

`color.dark.json` and `color.light.json` mirror every YAML token in this preset.
Keys are grouped into `color`, `shadow`, `bg`, `surface`, and `radius`
namespaces and use the Style Dictionary `{ "value": ... }` convention.

Image paths remain relative to the installed Jekyll site.
